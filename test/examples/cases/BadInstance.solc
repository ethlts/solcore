class a:Enum {
    function fromEnum(x:a) -> Word;
  }

data Color = R | G | B;

data Bool = False | True;

instance Bool : Enum {
  function fromEnum(b) {
      match b {
      | R => return 0;
      | G => return 1;
      };
  }
}


